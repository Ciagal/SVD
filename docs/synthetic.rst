Synthetic Dataset
=================

To evaluate the SVD implementation, synthetic low-rank matrices are generated.

A clean low-rank matrix is created and Gaussian noise is added.
This allows testing how well truncated SVD can denoise data
and approximate the original matrix.